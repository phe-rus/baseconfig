# Discussion log

Running log of the design conversation for BaseConfig (named Demoness until 2026-09-04 — see that entry below). Append dated entries; don't rewrite history — if a decision changes, add a new entry noting the change and why, rather than editing the old one away. Entries dated before 2026-09-04 say "Demoness" throughout because that was the project's real name at the time; this is left as-is rather than retroactively renamed, per this file's own rule.

## 2026-08-31 — Initial vision

Kickoff framing, roughly as given:

- PayloadCMS's *approach* is the model to borrow from (config-driven collections, auto-generated admin UI, hooks, access control) — but not the stack. Payload is Next.js, heavy, and pushes a paid cloud tier; it was never built for Cloudflare Workers/Pages.
- A prior attempt to build something similar directly inside `infra` or `seer` didn't land well and wasn't performing on par with what's needed — hence splitting this into its own project.
- New project name: **Demoness**, in its own monorepo at `~/projects/demoness`, fully open source.
- Explicit process requested: discuss everything first, then design (still no code), then figure out the dev approach, and only then start coding. This log and `CLAUDE.md` exist so that process survives across sessions instead of restarting each time.
- Preferred stack, stated directly: TanStack Start fully (all of its ecosystem — Router, Query, Store, Form, DB, etc.), Zod as the schema builder, D1, R2, KV, edge caching.
- Wants visual drag-and-drop editing, Payload-style, with a design concept inspired by Apple Freeform's app design and overall feel.
- Wants to reuse `infra` for auth/OAuth rather than rebuilding identity.
- Multi-tenancy: needs a strategy for "multi D1 into one D1" so the system can start simple and expand — i.e., a concrete story for scaling from a single shared database toward per-tenant or sharded databases without a rearchitecture. Needs to support single-tenant and multi-tenant use from day one.
- Rich text / editor approach: still to be discussed in CMS-specific terms, but the existing project rule (no third-party rich-text libraries, build on primitive DOM/contentEditable) is the working assumption carried over from `seer`.
- Role-based access: different people should be able to manage different sites/content with different permission levels — shape not yet decided.
- Mentioned wanting AI involved, without specifics yet.
- Target scale for staying entirely on Cloudflare's free plan: roughly 100 pages, 1000 blog posts, lots of images, and 500 active users, for a meaningful stretch of time before needing to pay for anything.

Nothing here is a locked decision yet — this is the opening framing the rest of the discussion phase works from. See `CLAUDE.md` for the running "open questions" list this should resolve into.

## 2026-08-31 — First round of decisions

Answers to the five open questions raised after the kickoff:

1. **D1 strategy: hybrid/sharded.** Start on one shared D1, shard out per tenant as they outgrow it. D1 free tier is 500 MB/database, 5 GB/account, 10 databases/account — confirmed via Cloudflare docs, so this isn't premature optimization, it's a real ceiling.
2. **Visual editing: freeform canvas is the default view**, with a switch to raw JSON view of the same content, and a switch to an API view showing the actual request/response. Three views, one underlying model.
3. **AI: in scope, provider-agnostic.** Ship with Workers AI and Gemini's free tier as initial providers; the provider layer must be pluggable so any other model/provider can be added later without a rearchitecture. Which AI *features* to build is still undecided.
4. **License: the Pherus Parley License.** Custom, MIT-based, named and themed after the Pirates of the Caribbean Code/Parley concept and Lord of the Mysteries. Decision on name and spirit only — actual clause text is a later step.
5. **Confirmed real Cloudflare free-tier numbers** (see `CLAUDE.md`'s table). Workers (100K req/day, 10ms CPU) and KV (1K writes/day) are the tight constraints; D1 and R2 have comfortable headroom for the stated target. The plan only works if rendered pages are served from edge cache on the common path and KV writes stay infrequent (KV reserved for sessions/small stuff only — confirmed).

## 2026-08-31 — D1 numbers corrected, RBAC decided

- Corrected a mistake in the first pass: D1's 500 MB/database and 5 GB/account/10-databases figures are all *Free*-plan numbers, not a "500 MB free vs 10 GB locked" split — the 10 GB/database and 1 TB/account/50,000-database numbers belong to Workers Paid ($5/mo). Also missed on the first pass: 5M rows read/day and 100K rows written/day (Free), both generous for the target.
- Important implication for the hybrid/sharded plan: 10 databases × 500 MB = the 5 GB account cap exactly, and the free plan hard-stops at 10 databases — there's no way to get more free D1 storage by sharding smaller. Once past 10 shards or 5 GB total, the answer is Workers Paid, not a bigger free allocation. This needs to be a designed decision point, not a surprise.
- **RBAC is dynamic** — roles/permissions are runtime-configurable rather than a fixed hardcoded set. Still open: what granularity the dynamic system operates over (site/org/collection-level or a mix) and how it interacts with D1 sharding.

## 2026-08-31 — AI feature scope

AI features, as stated: writing help for pages and blog posts, page design assistance, nav item generation/management, product management assistance, image improvements, and general automations beyond that. Which of these run on Workers AI alone vs. need Gemini or another pluggable model, and how they hook into the collection-config/hooks system, is still to be worked out.

## 2026-08-31 — Two D1 databases from the start (superseded, see next entry)

Not one shared DB: split into an auth/identity D1 and a separate CMS-content D1 (pages, posts, products, nav, tenant content). Hybrid/sharding still applies, but on the content side as tenants grow — identity is a separate concern. Open: whether the identity DB is Demoness's own, or Demoness reuses `infra`'s existing auth DB directly rather than standing up a duplicate identity store. Also worth remembering: the identity DB itself counts as one of the free plan's 10 databases/account, so it's not "free" headroom on top of the content-sharding budget.

## 2026-08-31 — Corrected: single D1, no sharding, tenancy via Organization plugin

Walked back the two-DB and sharded-multi-D1 plans above after clarifying the actual model: **one single D1 database**, no split. Auth uses better-auth wired to `infra`'s OAuth as the identity provider — the same pattern `seer` already runs (`export const auth = betterAuth({...})`) — plus an admin plugin. Multi-tenancy (one CMS serving many tenant websites) comes from better-auth's **Organization plugin**: each Organization is one tenant site, and auth, org/tenant data, and CMS content collections all live together in the one database. Since images go to R2, D1 only ever holds lightweight relational/text data, so a single database (500 MB free-tier cap, corrected down from an assumed 10 GB) is more than enough. Sharding to multiple databases is now a future escalation path only, not the starting architecture — most of the earlier open questions about sharding triggers and shard routing are moot for now.

## 2026-08-31 — RBAC/org model corrected, license drafted

- **RBAC/org correction:** access control is per-collection (Payload-style conditional access functions), not Organization-scoped. Organization/multi-tenancy is optional — the CMS can run single-tenant with no Organization concept at all, or multi-tenant. When multi-tenant, an Organization is more like a tag on content (akin to an author field) than a hard partition boundary, and there's a concept of the currently-active Organization that access rules and scoping can reference.
- **License drafted**, not just named: the Pherus Parley License now exists as an actual file (`LICENSE`). Operative terms are MIT, kept textually equivalent on purpose. The Parley (open hand before conflict, guidelines not rules) and Lord of the Mysteries (knowledge passed on freely) themes live in a non-binding preamble, explicitly separated from the legal text so the license stays unambiguous. Intended as Pherus's general open-source license, reusable beyond Demoness. Not yet lawyer-reviewed; no patent grant or trademark carve-out included.
- **Access-control API: exact match to Payload.** Each collection defines `access: { read, create, update, delete }`, each a function returning a boolean or query constraint, evaluated per-request. This closes out the RBAC-shape open question.

## 2026-08-31 — One unified AI layer, not per-feature providers

Clarified: "AI is AI" — one active provider/model, switchable, shared across all six AI features, not a separate integration or hardcoded provider per feature ("one AI, not 1000 AIs"). Providers stay dynamic/pluggable (Workers AI binding + Gemini free tier to start, any other provider addable later), but the abstraction the features call through is singular. Only remaining open item on AI: how the features hook into the collection-config/hooks system.

## 2026-08-31 — Collections/Globals as Zod configs; canvas redefined as a map, not the editor

Two decisions:

1. **Collections and Globals, exactly Payload's concepts** (repeatable types vs. singletons), authored as Zod-based "configs" — the term Demoness uses in place of Payload's field DSL. Same single-source-of-truth principle: one config generates DB schema, admin UI, and API together.
2. **The freeform canvas isn't the editing surface — it's a map/overview.** Revised from the earlier "freeform is the default editing view" decision: the canvas is a freely-arrangeable live overview of all pages (live-preview thumbnails, user-controlled ordering/positioning, shows connected structure to the Collections/Globals each page draws from — select a page to see and edit its connected configs). Actual editing of a Collection/Global item uses **Payload's Live Preview** pattern instead: fields on one side, live-rendered output updating in real time on the other. JSON view and API view still apply as alternate views of an item alongside Live Preview. Open: exactly how the map visualizes "connected structure," and how thumbnails stay current.

## 2026-08-31 — License finalized; AI now covers schema, not just content

- **License finalized without further back-and-forth**, per explicit instruction to just decide it and stop asking. Added one clause beyond base MIT — a trademark notice ("The Flag") protecting the "Pherus"/"Demoness" names from implying endorsement of a fork, same idea as Apache 2.0's trademark clause. Deliberately no patent grant, staying true to "closer to MIT." This is closed.
- **AI feature scope extended**: beyond filling in data within an existing Collection/Global item, AI can also help *create* a new Collection or Global (config) — operating at the schema/config-definition layer, not just the content layer.

## 2026-08-31 — Drafts & versioning: Payload-style, optional

Confirmed: follow Payload's draft-state model, and versioning too if turned on — both are opt-in per Collection/Global, not forced. Field-type taxonomy (text, richtext, number, boolean, select, relationship, array, blocks, upload, date, JSON) proposed as the default mirroring Payload's set, but not yet explicitly confirmed — treat as a working assumption until corrected.

## 2026-08-31 — No Cloudflare Images; publish-triggered edge caching, speed as an explicit goal

- **No Cloudflare Images** — too expensive. R2 only, well-structured (exact key/folder scheme still to be worked out). Image-improvement AI features route through Workers AI or another pluggable model instead of Cloudflare Images' transform API.
- **Caching model, decided**: the whole platform is edge-cached by default. Cache is populated/invalidated only when a publisher/editor actually publishes or updates — not on a timer, not per-request revalidation. Every other read (normal visitor traffic) is served from the edge cache, never touching D1 or backend logic. This is the concrete mechanism behind the earlier "must cache aggressively to fit the free tier" advice — now it's an actual decision, not just a constraint to keep in mind.
- **Extremely fast load times are an explicit goal**, not incidental — the cache-first model serves both the free-tier budget and the speed goal at once.

## 2026-08-31 — Dev approach: package structure, routing split, API surface, group() field type

First round of the "dev approach" discussion phase (design mockups are done — see the published canvas — this is architecture before code, per the discuss → design → dev approach → code order).

- **Monorepo package split, decided**: `packages/ui` (existing shadcn primitives) stays. New packages, folder name → npm scope: `demoness-fields/` (`@demoness/fields` — field-type definitions: Zod schemas + admin component mapping), `demoness-core/` (`@demoness/core` — `defineCollection`/`defineGlobal`, access control, hooks, generates DB schema + API + admin manifest from a config), `demoness-d1/` (`@demoness/d1` — Drizzle schema/client, migrations), `demoness-r2/` (`@demoness/r2` — asset upload, key scheme, serving), `demoness-ai/` (`@demoness/ai` — the one unified AI provider abstraction). Dependency order: `fields` → `core` → (`d1`, `r2`, `ai` as siblings `core` calls into) → `www` consumes everything plus `ui`.
- **ORM: Drizzle**, confirmed — D1-native, supports local-first dev via wrangler.
- **Payload-shape question resolved**: borrow Payload's *concepts* (fields, blocks, access API shape) but implement them the way Zod and TanStack idioms naturally want, not literal Payload API parity. This is why the field-config package is `@demoness/fields` rather than trying to be a Zod-flavored clone of Payload's own field DSL.
- **API surface: both.** Server routes (real HTTP endpoints, e.g. `GET /api/pages/:slug` — the public headless API, matches the API tab in the mockups) *and* TanStack Start server functions (for admin-internal calls) both stay in play — not one at the expense of the other. Both are thin wrappers over the same `@demoness/core` access-controlled operations, so there's one source of truth underneath regardless of which transport is used.
- **Routing/deployment: one app, one Worker, one deployment.** `/admin/*` is the dashboard (everything mocked in the canvas: Dashboard, Pages/Posts/Users lists, Edit's Hero/Layout/SEO/Settings tabs, Media, Headers, Site Settings) — authenticated, never edge-cached. `/` and everything else is the actual public site, rendering a page by walking its `layout` blocks through per-block-type renderers (the render-side counterpart of the Layout block system in the mockups). Cache rules must explicitly exclude `/admin/*` from the cache-first path decided in the entry above.
- **Field taxonomy: adopt Payload's full field-type list**, confirmed against Payload's own docs (payloadcms.com/docs/fields/overview) rather than inferred from the mockups alone — the earlier two passes at this list were both wrong (first too short, second invented two field types, `tags` and `slug`, that Payload doesn't actually have as distinct types):
  - **Data fields** (produce a DB column): `array`, `blocks`, `checkbox` (boolean), `code` (code-editor string), `date`, `email`, `group` (named), `json`, `number`, `point` (geo coordinates), `radio`, `relationship` (single or hasMany), `richtext`, `select` (single or hasMany), `tabs` (named), `text`, `textarea`, `upload`.
  - **Presentational fields** (admin-layout only, no DB column): `collapsible`, `row` (lay fields side by side), `tabs` (unnamed), `group` (unnamed), `ui` (blank slot for a fully custom admin component).
  - **Virtual**: `join` — computed two-way relationship (e.g. list every Post referencing an Author without storing the FK on the Author side); not backed by its own column.
  - **What the mockups actually map to, corrected**: Settings' "Tags" pills → `select` with `hasMany: true`, not a bespoke tags type. Settings' "Slug" (auto-derived from Title, editable, lock icon) → a plain `text` field with a custom admin component + a `beforeValidate`-style hook, matching Payload's own website-template pattern — also not a distinct field type. Hero's "Type" pill row and the Link field's "Link Type" toggle → `radio`. Hero's richtext area → `richtext`. Column Width and Appearance selects → `select`.
  - **Design insight, still holds**: Hero/Layout/SEO/Settings are best modeled as one named `tabs` field on the Pages collection config, not four separate top-level concerns — each tab just holds its own field set (Hero tab → a `group`, Layout tab → the `blocks` array, SEO tab → the plugin's fields, Settings tab → title/slug/parent/tags/authors).
- **Hooks, adopted at three tiers, confirmed against Payload's hooks docs** (payloadcms.com/docs/hooks/*):
  - **Collection hooks** (full CRUD lifecycle, in firing order): `beforeOperation` → `beforeValidate` (create/update) → `beforeChange` (create/update, data still unvalidated at this point) → *validation happens* → `afterChange` → `beforeRead` → `afterRead` → `beforeDelete` → `afterDelete` → `afterOperation`.
  - **Global hooks**: the same shape minus delete (a global is one singleton document, nothing to delete): `beforeOperation`, `beforeValidate` (update only), `beforeChange`, `afterChange`, `beforeRead`, `afterRead`.
  - **Field-level hooks** (attach to one field's config, fire during the same lifecycle as their parent operation): `beforeValidate`, `beforeChange`, `afterChange`, `afterRead`, plus `beforeDuplicate` (fires before beforeValidate/beforeChange specifically when a document is duplicated — lets a unique field like the slug rewrite itself so duplication doesn't collide).
  - **Root**: `afterError`, app-wide, not scoped to any one collection/global.
  - **Auth hooks dropped, decided**: Payload's six auth-collection hooks (`beforeLogin`, `afterLogin`, `afterLogout`, `afterMe`, `afterRefresh`, `afterForgotPassword`) and its two operation-replacement hooks (`refresh`, `me`) do not apply — auth is delegated entirely to better-auth via `infra`, which owns the whole login lifecycle externally. `Users` is a read-mostly profile extension of better-auth's own user table, not a Payload-style auth-enabled collection. If something ever needs to react to a login, that hook belongs on the `infra`/better-auth side, not as a Demoness collection hook.
  - **Context, confirmed against Payload's `req.context`**: request-scoped, `Record<string, unknown>` by default and extendable via TS declaration merging. Created once when an operation (one create/update/delete/read) starts and passed by reference through every hook that fires during it — collection/global hooks *and* every field-level hook on every field being touched share the **same** context object, not a separately scoped one per field, so a collection `beforeChange` can stash something a field's `afterChange` reads, and vice versa. Three uses, matching Payload: (1) fetch-once/reuse-later across hooks in one operation (e.g. call a 3rd-party API in `beforeChange`, read the result in `afterChange` without refetching), (2) infinite-loop guard — a hook that calls `core`'s own update on its document passes a context flag (e.g. `{ skipAfterChange: true }`) so it doesn't re-trigger itself, (3) passing extra data into an operation from the calling server function/route without inventing a dummy field just to carry it.
- **Plugins, confirmed against Payload's plugin docs**: a plugin is simply `(config) => config` — it receives the incoming Demoness app config and returns a modified one, free to add collections/globals, inject fields into *existing* collections, add hooks, add admin components/routes, add server routes. Registered as an ordered `plugins: [...]` array in the app's top-level config; each plugin transforms the config the previous one produced, and this all runs before validation/sanitization/defaults — same as Payload. Plugins follow the established package convention: folder `demoness-plugin-<name>/`, npm scope `@demoness/plugin-<name>`.
  - **The SEO tab is a plugin, not Pages-specific logic**: `@demoness/plugin-seo`, applied to a list of target collections (e.g. `['pages', 'posts']`), injects a `seo` tab into each — metaTitle/metaDescription/metaImage fields, the auto-generate-from-title hook, the character-count admin UI — all already built in the mockups. Directly modeled on `@payloadcms/plugin-seo`.
  - **The Settings tab's "Parent" field is a plugin too**: `@demoness/plugin-nested-docs`, matching Payload's official Nested Docs plugin — hierarchy + breadcrumb generation, rather than hand-rolled into `core`.
  - **Multi-tenancy (Organization support) is a plugin, decided**: `@demoness/plugin-multi-tenant`, not part of `@demoness/core` — matches Payload's own precedent (Multi-Tenant ships as a separate official plugin) and the earlier decision that multi-tenancy is optional. A single-tenant install never loads this plugin at all, rather than carrying always-present-but-sometimes-inert multi-tenancy code in core.
- **Top-level config: `export default buildConfig({...})`**, confirmed against Payload's config docs and adapted. Directly reused from Payload's shape: `collections`, `globals`, `plugins`, `hooks` (root `afterError`), `db` (required — the `@demoness/d1` adapter), `editor` (our own primitive contentEditable richtext, not Lexical), `secret` (required), `serverURL`, `admin`, `routes` (this is where the `/admin` vs public-root routing decision lives), `endpoints`, `upload` (ties to `@demoness/r2` defaults), `defaultDepth`/`maxDepth` (relationship population depth in API responses), `logger`/`debug`, `onInit`, `indexSortableFields`.
  - **Dropped, deliberately**: `graphQL` (we're server routes + server functions, no GraphQL — decided earlier), `folders`/`queryPresets`/`i18n`/`localization` (not designed yet, future), `telemetry` (not building any).
  - **`sharp` doesn't apply, flagged as a real constraint not a preference**: Payload's `sharp` option assumes Node.js — `sharp` is a native addon and cannot run on the Cloudflare Workers runtime at all. Any image resize/crop in `@demoness/r2` needs a WASM-based library instead (e.g. `@cf-wasm/photon`), not sharp. No decision needed yet, just noted before it's discovered mid-build.
  - **CORS: wide open (`'*'`) by default, decided.** Applies to the public *read* API only (`GET /api/pages/:slug` etc — content delivery, not mutations, which stay behind the admin's authenticated server functions). It's a genuinely public headless content API by design, so no per-consumer whitelist is required to fetch published content cross-origin.
  - **CSRF: kept, decided** — Demoness's own `core` config carries a `csrf` whitelist even though better-auth (via `infra`) owns the actual session-cookie lifecycle. Reasoning: defense in depth — the admin routes independently validate cookie origin rather than trusting better-auth's session handling alone. (Contrast with the auth-hooks decision above, which *were* dropped entirely — CSRF is kept specifically because it's a second independent check, not a duplicate of something better-auth already does.)
  - **Real Payload example confirms the proposed shape needed no changes**: `export default buildConfig({ secret, db: adapter(...), collections: [...] })`, config file conventionally at the project root — matches what was already proposed here.
- **`jobs`, added, optional** — confirmed against Payload's Jobs Queue docs (Tasks: slug + async handler; Workflows: ordered task groups, retry-from-failure; Jobs: queued instances; Queues: named groups, e.g. an `emails` queue running every 10 min vs. a nightly one). Config shape adopted as-is: `jobs: { autoRun: [{ cron, queue, disableScheduling? }], jobsCollectionOverrides }`. **Key divergence, and the reason to actually use it**: Payload's Jobs Queue exists because Node/serverless platforms have no native cron or queue primitives — Cloudflare Workers does (Cron Triggers, Cloudflare Queues), so `@demoness/core`'s `jobs` should be a thin adapter *onto* those platform features rather than reimplementing Payload's own bin-script/polling dispatch mechanism. Tasks/Workflows stay Payload-shaped (familiar, and DB-backed job records still make sense for status/retry visibility); scheduling and dispatch defer to Cron Triggers + Queues. **Not yet verified**: whether Cloudflare Queues is available on the free tier (Cron Triggers themselves are) — check against the free-tier reality-check table above before this becomes load-bearing; if Queues turns out to be Paid-only, `autoRun`'s cron-only path (no queue) may need to be the free-tier default. `jobs` itself is optional — a project with nothing to schedule omits the key entirely, same as `versions`/`drafts` being opt-in per collection.
- **Plugin naming: mirror Payload's official plugin names exactly** (just the scope changes, `@payloadcms` → `@demoness`), confirmed full official list: `plugin-seo`, `plugin-nested-docs`, `plugin-multi-tenant` (all three already matched what was already named above), plus newly identified for the future-candidates list: `plugin-form-builder`, `plugin-search`, `plugin-redirects` (useful once slugs can change), `plugin-sentry`, `plugin-stripe` (Products/ecommerce), `plugin-import-export`, `plugin-mcp` (ties naturally into the unified AI layer). **`plugin-cloud-storage` explicitly not adopted**: that's Payload's swappable-storage-backend abstraction (S3/GCS/Azure/etc.) — Demoness only ever targets R2, so that logic lives baked directly into `@demoness/r2`, not as a separate pluggable-backend plugin.
- **CRUD API shape**, confirmed against Payload's Local API and REST API docs:
  - **REST routes generated per collection/global**: `GET /api/{collection}` (list), `GET /api/{collection}/{id}`, `GET /api/{collection}/count`, `POST /api/{collection}` (create), `PATCH /api/{collection}` (bulk update via `where`) / `PATCH /api/{collection}/{id}`, `DELETE /api/{collection}` (bulk) / `DELETE /api/{collection}/{id}`; globals get only `GET /api/globals/{global}` and `POST /api/globals/{global}` (update — POST, not PATCH, matching Payload's own asymmetry). Query params: `depth`, `select`, `populate`, `limit`, `page`, `sort`, `where`, `joins` (`locale`/`fallback-locale` deferred with localization). Per the earlier CORS decision, only the `GET` routes are the wide-open (`'*'`) public surface — every mutating route requires an authenticated admin session regardless of CORS policy.
  - **Auth-collection routes dropped from `@demoness/core`'s Payload-style per-collection generator** (`/login`, `/logout`, `/me`, `/refresh-token`, `/unlock`, `/verify/:token`, `/forgot-password`, `/reset-password`) — but see the corrected, sourced entry below: this does *not* mean these operations don't exist, only that `core` doesn't need to generate them Payload-style, because better-auth already generates its own equivalent routes natively.
  - **`where` clause: adopt Payload's operator vocabulary as-is** — `equals`, `not_equals`, `greater_than`, `greater_than_equal`, `less_than`, `less_than_equal`, `like`, `contains`, `in`, `not_in`, `exists`, composed via nestable `and`/`or` arrays. Worth knowing and load-bearing for confidence in this choice: **Payload's own SQLite/Postgres adapters are built on Drizzle**, so this operator set already has a proven mapping onto Drizzle query builders — `@demoness/d1` is porting a known-working translation, not inventing one. Dropped: `near`/`within`/`intersects` (geospatial — no `point` field priority yet) and `all` (MongoDB-only, not relevant to D1).
  - **Internal/programmatic API accessor shape, decided**: typed per-collection, not Payload's flat `collection: 'slug'` string-param style. `core.collections.pages.find({ where, sort, limit, page, depth })`, `core.collections.pages.findByID({ id })`, `core.collections.pages.create({ data, user, overrideAccess })`, `core.collections.pages.update({ id, data })` (or bulk via `where`), `core.collections.pages.delete({ id })`, `core.collections.pages.count({ where })`; globals mirror as `core.globals.siteSettings.find()` / `.update({ data })`. Each collection's `data`/`where` shape is fully known to TypeScript through object-property access — no generic `<T>` or slug-string lookup needed to keep type safety, which is the reason this was chosen over Payload's literal shape (more idiomatic for the Zod/TanStack-first design this project already committed to). Options otherwise carried over from Payload's Local API: `depth`, `where`, `sort`, `limit`, `page`, `pagination`, `user`, `overrideAccess`, `showHiddenFields`, plus Demoness's own `context` (see the hooks entry above) in place of Payload's `req`.

## 2026-08-31 — Correction: login/logout/me/refresh-token do exist (auth-routes-dropped was wrong)

The "auth-collection routes dropped" line above was checked against `~/projects/infra`'s actual source (not generic better-auth docs) and turned out to overreach — it's corrected here rather than edited away, per this doc's own rule.

- **Confirmed from `infra`'s real source** (`infra/auth/auth.ts`, `infra/src/routes/api/auth/$.ts`, `accounts/lib/auth-client.ts`, `accounts/middleware/session-middleware.ts`): `infra` runs its own `betterAuth()` mounted at `/api/auth/*` via a TanStack Start catch-all route (`/api/auth/$`) that forwards the raw `Request` straight to `auth.handler(request)` — login, logout, get-session, refresh, etc. are all real, better-auth-native routes there, generated by better-auth itself, not by anything Payload-shaped. `infra` also runs the `oauthProvider()` plugin — it acts as an **OAuth2/OIDC provider** for other products, and `"seer"` is already listed in its `cachedTrustedClients`. Two integration patterns exist in that codebase: direct cookie-sharing via a shared `authClient` + cross-subdomain cookies (used by `accounts`, a same-org trusted subdomain app) vs. a full OAuth2 client flow (the pattern intended for a genuinely separate product — which is what `"seer"` already does). `infra`'s own better-auth instance has **no `organization` plugin** enabled.
- **Corrected decision**: Demoness needs **its own local `betterAuth()` instance**, same as `seer` — this was already the plan per the original "Stack direction" note in CLAUDE.md ("the same pattern `seer` already runs... with infra OAuth + an admin plugin") and shouldn't have been overridden. Demoness's instance is configured with a generic OAuth provider pointed at `infra` as the external identity provider, and needs to be added to infra's `cachedTrustedClients` (manual addition — `allowDynamicClientRegistration: false` on infra's side rules out self-registration).
- **So: login/logout/me/refresh-token do exist**, as better-auth's own auto-generated routes on *Demoness's own* auth instance, mounted at Demoness's own `/api/auth/*`, sitting alongside — not generated by — `@demoness/core`'s collection/global REST routes. `@demoness/core` still doesn't need a Payload-style per-collection auth-route generator (that part holds), because better-auth already provides the equivalent natively once Demoness runs its own instance.
- **Multi-tenancy unaffected**: since Demoness's better-auth instance is entirely local (not shared with infra), `@demoness/plugin-multi-tenant` enabling better-auth's `organization()` plugin doesn't depend on infra having one — infra having none doesn't block or change this plugin's design.
- **`Users` collection, refined**: it's a read-mostly profile extension of *Demoness's own local* better-auth user table (populated via OAuth sign-in from infra), not a direct mirror of infra's own user table.

Still open from here: R2 key/folder scheme (carried over, still TBD), how AI features hook into the collection-config/hooks system (carried over), and the map-canvas mechanics question (carried over) — plus newly: exact shape of the generated admin manifest `core` hands to the UI, and whether server routes are hand-written per collection or fully generated from config.

## 2026-08-31 — Access control, field-shape correction, and @demoness/ai on TanStack AI

- **Access control, confirmed against Payload's docs, adopted as-is**: functions are scoped per-operation (`read`/`create`/`update`/`delete` on a collection or global; the same shape again at field level for restricting one field independent of the whole document — e.g. only admins can edit a `role` field). A function receives `req` (carrying `user`) plus `id`/`data`/`siblingData`/`blockData`/`doc` depending on context, and returns either a `boolean` or a `Where` query constraint for row-level filtering — e.g. `read: ({ req }) => req.user ? true : { status: { equals: 'published' } }` lets logged-in users see everything while anonymous visitors only see published docs, enforced as a query constraint rather than a post-hoc filter. During the login/session check specifically, doc-scoped args (`id`/`data`/`doc`) are undefined — that's a general-capability check, not a per-document one. Matches the typed-per-collection accessor already decided: `access: {...}` lives inside `defineCollection`/`defineGlobal`.
- **Field-shape correction — real, not a preference change**: Payload fields are **plain objects carrying a `type` string** (`{ name: 'pageTitle', type: 'text', required: true }`), confirmed from Payload's own field docs — not builder-function calls (`text({...})`) as had been assumed and never actually checked in earlier entries here. Resolution, honoring both "follow Payload" and "best suited to TanStack/Zod": `@demoness/fields` still exposes builder functions (`text({...})`, `blocks({...})`, etc.) for ergonomic, type-inferred authoring, but each one **produces that exact Payload-shaped plain object** underneath — so the config a collection actually holds at rest is structurally identical to Payload's (portable mental model, easy to read/serialize/diff), while authoring it reads like idiomatic TanStack/Zod code. The builder function is sugar over the object, not a divergent shape.
- **`@demoness/ai` is built on `@tanstack/ai`, not hand-rolled** — general principle stated and adopted: **TanStack libraries take priority over any other third-party library whenever one is needed**, and TanStack AI fits directly. Confirmed shape: a server route (`chat()` handler in a TanStack Start server route) paired with a client hook (`useChat()`), AG-UI-compliant streaming, official provider adapters including **Gemini** (already matches the earlier provider decision). **Workers AI is not in TanStack AI's official adapter list** — the architecture is provider-extensible, so this needs a custom adapter written for the Workers AI binding, a build task rather than a blocker. `@tanstack/ai-mcp` provides a host-side **MCP client** — kept conceptually distinct from `@demoness/plugin-mcp` (a future MCP *server* exposing `core`'s own CRUD operations as tools to external AI clients like Claude Desktop): complementary, not the same package or the same direction of integration.
- **`@demoness/d1` schema generation, confirmed against Payload's actual Drizzle source** (`packages/drizzle/src/schema/traverseFields.ts` in `payloadcms/payload`), adopted as-is:
  - **`array`** → a dedicated child table: `_order` (integer, sequencing) + `_parent_id` (FK to the parent row, `onDelete: cascade`).
  - **`blocks`** → **one table per block type**, not one shared table — each with `_order`, `_parentID`, and `_path` (tracks nested position, handles blocks-within-blocks/arrays). Table named `{rootTable}_blocks_{blockSlug}`; the parent gets a "many" relation to each block-type table.
  - **`group`** → **no separate table** — flattened into prefixed columns on the same parent table (`groupName_subfieldName`), recursively. Denormalized, not relational.
  - **`relationship`** (single) → a plain FK column (`{field}_id`) on the parent table, `onDelete: set null`. `hasMany` → a junction table.
  - **Naming conventions adopted directly**: `_rels` suffix for relationship junction tables, `_v` for versions (ties into the still-open drafts/versioning storage shape), `_locales` (deferred with localization).
  - **Why join-tables-by-default is an even easier call for Demoness than for Payload itself**: Payload exposes `blocksAsJSON` as an opt-in escape hatch specifically because a typical Payload site queries the database on *every visitor request*, making join overhead on blocks a real hot-path cost. Demoness's whole architecture is cache-first (decided earlier) — D1 is only touched on publish/write, never on normal visitor traffic — so that performance motivation barely applies here. Relational blocks-by-default is the comfortable default for Demoness, not merely the ported one.

## 2026-08-31 — Admin/consumer split, deliberate divergence from Payload's `_rels`, and the `types/` folder correction

- **Admin-UI structure must not leak onto the consumer/data side.** While implementing `@demoness/d1`'s schema builder against `@demoness/fields`' `AnyField`, caught that `BaseField.admin` (and therefore every field object) carried a **mandatory** `component: string` — set by every one of the ~18 builders (`withBase("text", name, "TextInput", ...)`) even though nothing yet read it. That forced any consumer of a field config — `@demoness/d1`'s schema builder, and eventually the public-site renderer — to carry admin-panel-component naming it has no use for. **Fixed**: `FieldAdmin.component` and `BaseField.admin`/`AnyField.admin` are now optional; `withBase` no longer takes a component-name argument at all. Which component renders a given field type is now purely an admin-app concern (a lookup keyed on `field.type`), not something baked onto the field object as a default. The one field type that legitimately keeps a required `component` is `ui` (`field-types/ui.ts`) — its entire purpose *is* being a named slot for a custom admin component, so that's not a leak, it's the field type's actual definition.
- **Types get their own `types/index.ts` folder, never a flat `types.ts`, never declared inline in a `.tsx`.** Re-confirmed against `infra`'s real structure (`infra/infra/domains/auth/types/index.ts`) — `types` is a sibling folder to `config/`/`hooks/`/`views/` at the domain root, not nested inside `config/`. Moved every existing flat `{domain}/config/types.ts` (dashboard, media, pages, site-map, shell) to `{domain}/types/index.ts`, and `packages/demoness-d1/src/schema/types.ts` to `schema/types/index.ts`. Also extracted two inline `.tsx` type declarations that predated this rule being made explicit: `EditChromeProps`/`DocTab`/`FieldTab` out of `pages/views/edit-chrome.view.tsx`, and the admin tab-route's field-tab union (renamed `PageAdminTab`, since it's a different, wider set than `EditChrome`'s own `FieldTab` — it includes `"live-preview"`) out of `routes/admin/$collection/$id/$tab.tsx` — both now live in `domains/pages/types/index.ts`.
- **`@demoness/d1`'s per-field junction tables are a deliberate, documented divergence from Payload's shared `_rels`.** Payload stores every `hasMany` relationship/upload field across a whole collection in **one** polymorphic `_rels` table (parent id, field name, and a set of nullable target-id columns — one per possible related collection). `buildD1Schema` instead builds **one junction table per `hasMany` field** (`${fromTable}_${fieldName}_rels`, a plain `parent_id`/`related_id` pair). Simpler to read and query (no polymorphic nullable-column fan-out, no need to know which target-id column is "live" for a given row), at the cost of one extra table per hasMany field instead of one shared table per collection — an acceptable tradeoff given D1's free-tier table-count headroom and that Demoness collections are hand-authored, not user-defined at scale. Not revisited unless a real multi-collection polymorphic-relationship use case demands it.
- **String primary keys (`text("id").primaryKey()`), not integer autoincrement**, on every generated table — matches Payload's own default (`id` is a generated string, e.g. `pg_8f2ac1` as already used in Stage 1's static mock data) and avoids the cross-collection FK-ordering problems integer autoincrement would create with the worklist/thunk-based table-building approach in `buildD1Schema`.
- **`where`-clause → Drizzle translation, done and tested** (`src/query/where-to-drizzle.ts`): the confirmed operator vocabulary maps onto real, verified `drizzle-orm` exports (`eq`/`ne`/`gt`/`gte`/`lt`/`lte`/`like`/`inArray`/`notInArray`/`isNull`/`isNotNull`/`and`/`or`, checked against the installed package's own `.d.ts`, not guessed). `contains` auto-wraps its value in `%...%` (substring-match sugar); `like` passes the caller's raw pattern through unchanged. Multiple operators on one field, and multiple fields/`and`/`or` keys at the same `Where` level, are implicitly ANDed together — matches Payload's own semantics, verified by rendering actual SQL text via `drizzle-orm/sqlite-core`'s `SQLiteSyncDialect().sqlToQuery()` (no live DB needed) in `__tests__/where-to-drizzle.test.ts`.
- **Migration tooling: investigated, deliberately deferred to Stage 4, not a Stage 3 gap.** Checked `drizzle-kit`'s own bundled source (`prepareFromExports3` in the installed `drizzle-kit@0.31.10`'s `bin.cjs`) to see how its `schema` config option actually loads a schema file — it does `Object.values(moduleExports)` **one level deep** and keeps whatever satisfies `is(value, SQLiteTable)`. That means drizzle-kit can only see **statically, top-level exported table objects** from the schema file it's pointed at. `buildD1Schema`'s output (`{ tables: Record<string, Table> }`, built at runtime from whatever `SchemaSource[]` it's given) can't satisfy that — ESM has no way to turn a runtime record into dynamic named exports. Real migration generation needs a **codegen step**: run `buildD1Schema` against the real collection/global configs and write a literal `.ts` file with one `export const <table> = sqliteTable(...)` per table for drizzle-kit to import — this is, concretely, what Payload's own Drizzle package does under the hood, not a shortcut being skipped here. That codegen step is only meaningful once `@demoness/core` (Stage 4) actually has real collection/global configs to generate from, so it's deferred there rather than faked here with placeholder collections. `packages/demoness-d1/package.json`'s `drizzle-kit` devDependency stays in place for when that lands.

## 2026-09-04 — Renamed Demoness → BaseConfig; Stages 0–3 deleted, reset to design-review

Per explicit instruction: "stop everything... rename it from demoness to baseconfig... delete all the packages... except the ui as well as domains and blocks folders... convert www to www... lets go back to our knowledge and review and design... correct our knowledge... baseconfig is the name of the cms."

- **Project renamed: Demoness → BaseConfig.** Every reference in `CLAUDE.md` and `docs/PLAN.md` updated (`@demoness/*` → `@baseconfig/*`, `demoness-*` folders → `baseconfig-*`, `demoness.config.ts` → `baseconfig.config.ts`, root `package.json` name → `baseconfig`, `wrangler.jsonc`'s `name`/`database_name`/`bucket_name` → `baseconfig`/`baseconfig-dev`/`baseconfig-media-dev`, `LICENSE`'s trademark clause → "Pherus" or "BaseConfig"). This document's own historical entries above are left saying "Demoness" — that was the real name at the time, and this file's own rule is to append corrections, not rewrite history.
- **Deleted, not merely renamed**: `packages/demoness-fields`, `demoness-core`, `demoness-d1`, `demoness-r2`, `demoness-ai` (all of Stage 2's field builders + tests, most of Stage 3's D1 schema generation + tests, and an in-progress Stage 4 `@demoness/core` that existed on disk but wasn't yet reflected in `docs/PLAN.md`'s checkboxes) — gone entirely. Also deleted: `www/src/domains/*` and `www/src/blocks/` (Stage 1's static-data admin shell), and `www/src/routes/admin/*` (necessarily — those routes exclusively rendered the now-deleted domain views, so they'd have been dead imports otherwise; not explicitly named in the instruction but a direct, unavoidable consequence of deleting `domains/`). **Kept**: `packages/ui` untouched, `www/collections/` and `www/globals/` (README placeholders only, reworded).
- **`apps/web` renamed to `www`** — first `git mv`'d to `apps/www` (rename detected cleanly by git), then moved again (by the user, directly on disk) up to the repo root as just `www/`, with root `package.json`'s `workspaces` updated from `["apps/*", "packages/*"]` to `["www", "packages/*"]` and `engines.node` bumped to `>=24`. `www/package.json`'s own `name` field updated from `"web"` to `"www"` to match. `routeTree.gen.ts` (TanStack Router's generated file) was deleted and regenerated via `bun run build` since it referenced the now-deleted admin routes — confirmed the build succeeds and the regenerated file only contains the root `/` route.
- **`packages/ui` renamed `@workspace/ui` → `@baseconfig/ui`** (also 2026-09-04, user-initiated) — the template's default scope name replaced to match the rest of the `@baseconfig/*` packages. Propagated across ~50 internal import statements in `packages/ui/src/components/*`, `www/package.json`'s dependency entry, `www/components.json`'s aliases, and `CLAUDE.md`'s package table. `bun install` and `turbo typecheck` both clean afterward.
- **Safety net**: none of this was committed to git before today's session (only the original template-scaffold commit existed) — everything from Stage 0 through Stage 3, plus all the `packages/ui` additions, was sitting uncommitted in the working tree. Before deleting anything, the entire pre-rename state was committed as a checkpoint (commit message: "checkpoint: snapshot pre-baseconfig-rename state (Demoness Stage 0-3 + UI)") specifically so the deleted work stays recoverable from git history rather than being permanently lost.
- **`docs/PLAN.md`'s Stage 0–3 sections rewritten**: the retrospective "done 2026-08-31, here's exactly what got built" narrative (which described code that no longer exists) was replaced with forward-looking, unchecked TODOs — same goals, `@baseconfig/*` naming, and the real lessons/gotchas from the deleted build carried forward as notes (e.g. Stage 2's `row`/`collapsible` having no `name` property, Stage 3's migration-tooling blocker needing a codegen step). Stages 4–11 were already future/unstarted and needed only the mechanical rename.
- **Not yet done**: the actual "review and design" pass this reset was meant to set up — this entry only covers the rename/reset mechanics. Nothing about the architecture itself (package split, field taxonomy, schema-generation approach, etc.) has been re-examined or changed; it's carried forward as-is from the pre-rename decisions, pending that review.

## 2026-09-04 (later) — `@baseconfig/core` structure decided; first real dashboard UI built and previewed live

Continuing the same day's review pass, several concrete decisions landed, then a first slice of real code was authorized and built:

- **`@baseconfig/fields` merges into `@baseconfig/core`** — no separate fields package, matching how Payload itself has no separate fields package (field types are just part of core's config surface). Core stays UI-free (no React/JSX) and gets exact internal structure: `src/admin/{types.ts, index.ts, fields/ (one file per field type), forms/, functions/, views/{dashboard/{asidebar.tsx, headers.tsx, index.ts}, index.ts}, documents/{documents.tsx, index.ts}}` sitting alongside `src/api/{auth/, storage/, index.ts}` — one package, not split into `core`+`admin`, with bundle discipline enforced by subpath structure (an API-only consumer only ever imports the `api` side) rather than a package boundary. The consumer's (`www`) own files stay a thin shell: `routes/(admin)/route.tsx` (pathless layout, just `<Outlet/>`) and `routes/(admin)/admin/$.tsx` (catch-all that mounts the whole dashboard), modeled on Payload's real generated `page.tsx`/`route.ts` files (fetched and read from `payloadcms/payload`'s actual GitHub source, not assumed) — both are ~15 lines, nearly 100% re-exported logic.
- **Dependency versions: exact-pinned, not ranged.** Every `package.json` in the workspace now pins exact versions (verified against the live npm registry at the time, not copied from whatever was already resolved locally under old `^`/`latest` ranges — that reflects the old range's ceiling, not true latest). Manual bumps only, from here on.
- **No comments in code or config**, full stop — stricter than this file's usual "only when the why is non-obvious" default.
- **Library packages build with `tsdown`** (rolldown-based `tsup` successor) once any of them need a real build step — not pre-emptively scaffolded, set up per-package as it's actually built. `packages/ui` stays source-only (no build step) unless that changes.
- **`biome.json` replaced wholesale** with the user's own cross-project standard config — tabs, single quotes, no semicolons except where needed, no trailing commas, a hand-picked linter rule set instead of `recommended`. Superseded this file's own earlier, now-wrong description of the formatting convention (double quotes/2-space/es5 commas) — go by the real `biome.json`, not old prose here.
- **First real code, explicitly authorized** ("wire it up to the consumer www so I can preview it live"): built `packages/baseconfig` for real (package.json, tsconfig.json, `src/admin/{types.ts, index.ts, views/{index.ts, dashboard/{asidebar.tsx, headers.tsx, index.tsx}}}`) — placeholder data only, no real config/engine wired in, sidebar + header + stat tiles + collections/globals grid, styled entirely through `packages/ui`'s real shadcn CSS-variable tokens (`bg-sidebar`, `bg-card`, `text-muted-foreground`, etc.) via Tailwind utility classes, not inline hex. Wired into `www` via `routes/(admin)/route.tsx` + `routes/(admin)/admin/$.tsx`. Caught and fixed two real bugs along the way: `www/tsconfig.json`'s path aliases for both `@baseconfig/ui` and `@baseconfig/core` had one extra `../` (latent since the `www` move to repo root — silently masked for `@baseconfig/ui` because Node's real `package.json` `exports` map resolution covered for it via the workspace symlink, until `@baseconfig/core`'s internal self-imports had no matching wildcard export and both resolution paths failed at once). Fixed by correcting the path depth and switching `@baseconfig/core`'s internal cross-file imports to relative paths instead of bare self-imports, keeping its `package.json` `exports` map to just the one public `./admin` subpath. Verified for real, not assumed: `turbo typecheck` clean across all three packages, `bun run build` succeeds, and `curl`ing a live `bun run dev` server against `/admin/dashboard` returns real server-rendered HTML with the expected content (confirmed via `grep`, not just a 200 status).
- **A dispatched research fork significantly overstepped its scope** during this session (asked to "research PayloadCMS's structure only, do not design anything") — it published two design-canvas artifacts and edited `CLAUDE.md` unprompted, and separately made two real regressions unrelated to its task in `www/package.json` (dropped the `typecheck` script, silently pinned unverified exact dependency versions) and stripped a comment from `wrangler.jsonc`. Caught via `git diff` review before accepting anything from it, and fixed by hand (the admin-template artifact itself turned out to be solid work and was kept; the unrelated regressions were reverted, then superseded anyway by the exact-pin decision above done correctly against the live registry).

## 2026-09-05 — Sidebar removed from the admin shell; route naming convention re-demonstrated; then a real "sticky header not working" bug hunt exposed three stacked dev-loop/toolchain bugs

Continuing the same admin-shell build from 2026-09-04, in direct-edit iteration (the user editing files by hand between turns, not only requesting changes verbally):

- **Sidebar explicitly rejected as not matching the established design** ("current the design doesnt have any resumblence to our design and it lucks tsdown we dont have sidebar in our desings") — the `asidebar.tsx`/`AdminNavItem`/`AdminNavSection` admin-shell sidebar built earlier was deleted. The admin shell was restructured from `views/dashboard/{asidebar.tsx, headers.tsx, index.ts}` to the current real shape: `views/route-root.tsx` (bare Fragment: `<Headers/>` + `{children}`, no wrapping div, no sidebar) + `views/headers/index.tsx` (the topbar-only chrome) + `views/config/{index.tsx, documents.tsx}` (the dashboard content, `RouteComponents` → `<article>` → `<Documents/>`). `tsdown` was added as the build tool for `packages/baseconfig` per the already-decided convention. See `CLAUDE.md`'s `@baseconfig/core` section for the exact current file tree — this entry only records *why* it changed, not the resulting shape (which drifts as the code does; `CLAUDE.md` stays the current-state source of truth).
- **Route file naming convention re-demonstrated, same rule as 2026-09-04, now applied to `www/src/routes/(admin)/admin/route.tsx`** (a new pathless-layout file introduced when `RouteRoot` was moved to wrap only `/admin/*`, not the whole `(admin)` group — the outer `(admin)/route.tsx` is now a trivial `<Outlet/>` passthrough). `www/src/routes/(admin)/admin/route.tsx` mounts `RouteRoot` around `<Outlet/>`; `admin/$.tsx` mounts `RouteComponents` directly, both following the existing `RouteComponent`/`RouteComponents`-only naming rule with zero local re-wrapping.
- **The bug hunt** ("dont write any code invistage and tell me why is my header not sticky top-0 not working"), investigate-only per explicit instruction, then fixed once diagnosed and approved piece by piece. Three genuinely separate, stacked bugs — fixing only one produced no visible change, which is exactly why this took several rounds to actually resolve:
  1. **Stale build.** `www` only ever consumes `@baseconfig/core`'s published `dist/admin/index.mjs` (per `package.json`'s `exports` map), never `src/` directly. `packages/baseconfig/package.json` had no `"dev"` script, so `turbo dev` (root `bun run dev`) silently only started `www`'s `vite dev` — `dist/` was still the output of the one manual `tsdown` build run from over an hour earlier, and still contained an entirely different, older header/layout implementation (a fixed `h-screen` shell, `overflow-auto` content pane, non-sticky `h-13` header) that predated the sticky-header work. Confirmed empirically by comparing file mtimes (`dist/admin/index.mjs` vs. `headers/index.tsx`/`route-root.tsx`) and by `grep -c sticky` on the actual served bundle (0 matches). **Fix, approved and applied**: added `"dev": "tsdown --watch"` to `packages/baseconfig/package.json`; `turbo dev` picks it up automatically once a workspace declares the script — no `turbo.json` change needed, since its `dev` task was already `{ cache: false, persistent: true }`.
  2. **Tailwind never scanned `packages/baseconfig` at all.** Even with a fresh build, the compiled CSS (verified by `curl`ing the dev server's live `globals.css` output and grepping for `.sticky`/`.top-0`) had zero rules for classes used only inside `packages/baseconfig`. Root cause: `packages/ui/src/styles/globals.css`'s `@source` directives (which extend Tailwind v4's automatic, `.gitignore`-respecting project scan — and `dist/` being gitignored means built output is never a fallback) had wrong relative paths — one meant for `www/` was one `../` short (resolved to nonexistent `packages/www`), another pointed at a `packages/components` that never existed, and none reached `packages/baseconfig/src/` at all. **Fix, approved and applied**: corrected to `../../../../www/**/*.{ts,tsx}` and `../../../baseconfig/src/**/*.{ts,tsx}` (paths are relative to `globals.css`'s own location, `packages/ui/src/styles/`).
  3. **Found along the way, not the original bug but a real regression from fix #1**: turning on `tsdown --watch` started leaking stray `.d.ts` files (`avatar.d.ts`, `button.d.ts`) directly into `packages/ui/src/components/` on every rebuild — polluting a package meant to stay source-only/build-free. Root cause: `packages/baseconfig/tsconfig.json` had a redundant `paths` entry (`"@baseconfig/ui/*": ["../ui/src/*"]`) mapping straight at `packages/ui`'s raw source, even though `packages/ui/package.json` already declares proper subpath `exports` (`./components/*`, `./hooks/*`, `./lib/*`, etc.) that resolve the same imports correctly through normal workspace-package resolution. The redundant alias pulled `packages/ui`'s files directly into `packages/baseconfig`'s own TypeScript program (not through package resolution), so `tsdown`'s `dts: true` step emitted their declarations next to their own source instead of confined to `packages/baseconfig/dist/`. **Fix, approved and applied**: removed the redundant `@baseconfig/ui/*` entry from `packages/baseconfig/tsconfig.json`'s `paths` (kept the legitimate self-referencing `@baseconfig/core/*` entry), deleted the two stray already-generated `.d.ts` files.
- All three fixes verified empirically, not assumed: re-fetched the live compiled CSS after the `@source` fix and confirmed `.sticky { position: sticky; }` and `.top-0 { top: 0px; }` are both now present; confirmed `tsdown --watch`'s log shows rebuild-on-save (`Found .../config/index.tsx changed, rebuilding...`) firing correctly on a live source edit; confirmed the stray `.d.ts` files are gone and `git status` shows `packages/ui` clean again.
- **One pre-existing, separate, still-unfixed minor issue surfaced during the same audit**: `headers/index.tsx`'s `z-35` is not a valid Tailwind v4 default z-index scale value (the scale is only `0/10/20/30/40/50/auto`) — it silently generates no CSS. Doesn't block `position: sticky` itself (that's a stacking-context concern, not a positioning one), left as-is since it wasn't part of what was asked to be fixed.
- **Next requested, not yet started**: design work has moved on to the collection list view and the document editor view (per `CLAUDE.md`'s `## Open questions`), plus a decided-but-undesigned direction that `@baseconfig/core` should own/re-export TanStack Router's `Link` and TanStack Start's server-function primitive, since both the CRUD API and all admin navigation live in `core`, not in the thin `www` shell. No plan/stages written for either yet — per `docs/PLAN.md`'s own rules, waiting for explicit go-ahead before starting.

## 2026-09-05 (later) — Staged plan for the collection/editor views, corrected: one permanent admin catch-all, dispatch logic lives in core, verified against Payload's real source

A first pass at staging the collection-list and document-editor work (split into Sub-stages 1A/1B/1C in `docs/PLAN.md`) proposed splitting `www/src/routes/(admin)/admin/$.tsx` into multiple file routes as each view got built (`admin/index.tsx`, `admin/$collection/index.tsx`, `admin/$collection/$id/$tab.tsx`). **Corrected, not just refined**: "1A is wrong there will only be one tsx which is a caty all in admin the entire logic for it remains in the library side... figure out a way to mimic how paylaodcms does it." `www` gets exactly one admin route file, period — matching how Payload's own consumer-side file works, verified against `payloadcms/payload`'s real source (not assumed from docs prose), fetched via `gh api repos/payloadcms/payload/contents/...`:

- **`packages/next/src/adapters/views.ts`**: Payload's actual Next.js catch-all page (`app/(payload)/admin/[[...segments]]/page.tsx` in a real Payload project) is one file forever. It calls `RootPage({ config, params: Promise<{ segments: string[] }>, searchParams, importMap })`, which is just `renderRoot({ ...props, adminViews: defaultAdminViews, initReq, notFound, redirect })` — a thin pass-through into `@payloadcms/ui`.
- **`packages/ui/src/views/Root/index.tsx`**'s real `renderRoot()`: takes the raw `segments` array, resolves `collectionConfig`/`globalConfig` by matching `segments[1]` against `config.collections`/`config.globals` slugs (with `segments[0] === 'collections' | 'globals'` gating which), builds a request (`initReq`), calls `getRouteData(...)`, then renders the resolved `DefaultView` wrapped in `DefaultTemplate`/`MinimalTemplate`/no-template depending on `templateType`.
- **`packages/ui/src/views/Root/getRouteData.ts`**'s real dispatch logic — the actual heart of it: a `switch (segments.length)` (cases `0`/`1`/`2`/`default`), branching further on literal segment strings (`'collections'`, `'globals'`, `'trash'`, `'hierarchy'`, `'verify'`, `'create'`) and on whether `collectionConfig`/`globalConfig` resolved, producing `{ DefaultView, viewType, documentSubViewType, routeParams: { collection, global, id, versionID, token }, templateType, templateClassName, viewActions }`. `DefaultView` is picked from a small per-viewType component registry (`adminViews`/`oneSegmentViews`), not hardcoded per route. Real confirmed URL scheme: `/admin` (dashboard), `/admin/collections/:slug` (list, redirects to `/admin` if no matching collection), `/admin/collections/:slug/:id` (edit — default sub-view), `/admin/collections/:slug/:id/versions`, `/admin/collections/:slug/:id/api` (`getDocumentViewInfo` turns trailing segments into `documentSubViewType`), globals mirrored under `/admin/globals/:slug`.
- **BaseConfig's mimicked version, decided**: `admin/$.tsx` forwards its TanStack Router splat (a single rest-of-path string — needs splitting on `/` inside core, since TanStack doesn't hand back a pre-split array the way Next's catch-all params do) into one root-dispatch export from `@baseconfig/core/admin`. That function owns the entire segments→config-lookup→viewType→component-registry→template pipeline internally, scaled to what BaseConfig actually has (dashboard/list/editor today, more as they're built) rather than Payload's full feature set. Proposed URL scheme mirrors Payload directly: `/admin`, `/admin/collections/:slug`, `/admin/collections/:slug/:id`.
- **One deliberate, flagged divergence — not blindly copied**: Payload's URL-addressable document sub-views (`versions`, `api`) are a structurally different concept from BaseConfig's Hero/Layout/SEO/Settings tabs (the existing "one named `tabs` field" design insight, `CLAUDE.md`'s Field taxonomy section) — the former are meta-views *about* a document, the latter are a grouping *within* the document's own content. Decided: Hero/Layout/SEO/Settings stay client-side tab state inside the single editor view component, not further URL segments — while a future `versions`/`api` sub-view (if built at all) would follow Payload's real segment-based pattern. `CLAUDE.md`'s Routing & folder layout section and `docs/PLAN.md`'s Sub-stage 1A were both updated to match this corrected design — see there for the current real plan, this entry only records the research trail and why the first pass was wrong.

**Sub-stage 1A built and verified same day.** `packages/baseconfig/src/admin/views/config/index.tsx`'s `RouteComponents` now reads the router's splat param (confirmed via `router-core`'s real source, `node_modules/.bun/@tanstack+router-core@.../dist/esm/path.js`, that a `$` file route exposes it as both `params._splat` and `params['*']` — used `_splat` via `useParams({ strict: false })`, since `RouteComponents` is a generic component with no access to `www`'s specific `Route` object, matching the "no local re-wrapping" rule), splits it, and dispatches through a new `get-route-data.ts` (`views/config/get-route-data.ts`) mirroring Payload's real `getRouteData` shape at BaseConfig's current scale: 0 segments → `dashboard`; `collections/:slug` → `list`; `collections/:slug/:id` or `globals/:slug` → `document`; anything else → `not-found`. The known collection/global slugs are the same static arrays `Documents` already renders (`collections`/`globals`, now `export`ed from `documents.tsx` and imported by `get-route-data.ts` rather than duplicated) — real config-driven validation is Stage 2/4's job, this stays placeholder-data-consistent with the rest of Stage 1. `www/src/routes/(admin)/admin/$.tsx` needed **zero changes** — it already imported `RouteComponents` directly with no wrapping, which turned out to be exactly the right shape for this all along.

A new `packages/baseconfig/src/admin/link.ts` re-exports `@tanstack/react-router`'s `Link` (`@baseconfig/core` now depends on `@tanstack/react-router`, pinned to the *exact same* version `www` already uses — `1.170.32`, not independently "latest" — deliberately, so bun dedupes to one physical install and both packages share the same router module instance/context; a version mismatch here would risk two separate router contexts). The dashboard's Collections/Globals cards (`documents.tsx`) now navigate for real via this `Link`, using `to='/admin/$'` with `params={{ _splat: `${segment}/${slug}` }}` — discovered mid-build that `Link`'s `to` prop is strictly typed against the actual registered route tree once `www`'s own `tsc` type-checks `packages/baseconfig`'s source directly (it does — `www/tsconfig.json`'s path mapping points straight at `../packages/baseconfig/src`), so an arbitrary template-string `to` doesn't type-check; the splat-route form does, and is the correct approach for a route shaped like this one anyway. Also restructured each card so the decorative "+" button (add-item overlay, still inert/no handler) is a sibling `absolute`-positioned div next to the `Link`'s `<a>`, not nested inside it — nesting a `<button>` inside an `<a>` is invalid HTML; verified via the real rendered SSR output (`curl`, not assumed) that the anchor closes cleanly before the button's wrapper div opens.
- **Types extracted to `src/admin/types/index.ts`** (`ViewType` as a `const`-object-plus-`as const`, `RouteParams`, `RouteData`) — the first real use of the types-folder convention inside `packages/baseconfig`, flagged as a placeholder TODO in `CLAUDE.md` a few turns earlier and now filled in with real content.
- **Verified for real, not assumed**: `packages/baseconfig` and `www` both typecheck clean directly (`bun run typecheck` per-package); `curl`ed the live dev server for `/admin`, `/admin/collections/pages`, `/admin/collections/pages/abc123`, `/admin/globals/headers`, and `/admin/collections/bogus-slug` and confirmed each resolves to the correct view text server-rendered in the real HTML (not just "no error thrown"); confirmed the dashboard cards' `href`s are all correct (`/admin/collections/pages`, `/admin/globals/headers`, etc.); confirmed via the raw HTML that the anchor/button nesting fix actually worked.
- **Two pre-existing, unrelated typecheck failures surfaced while verifying** — neither touched this session (confirmed via `git status`/`git diff --stat` showing both clean before any edits), so left alone rather than silently fixed: `packages/ui/src/components/spinner.tsx` (a real `string | number` vs `number | undefined` type error) and `packages/baseconfig/src/admin/views/route-root.tsx` (needs `import type { PropsWithChildren }`, `verbatimModuleSyntax`). Both currently block a clean top-level `turbo typecheck` (turbo's dependency graph gates `core`'s typecheck task on `ui`'s succeeding first, and `www`'s typecheck directly re-checks `route-root.tsx`'s source) — per-package `bun run typecheck` was used to verify 1A instead. Flagged in `docs/PLAN.md` for the user to decide whether/when to fix.

## 2026-09-05 (later still) — 1A corrected: dispatch moved from an in-component hook into a real TanStack `loader`, URL scheme flattened

The first pass at 1A above ("makes no sense... remove it... reevaluate the logic... how does payload do it, not the example projects, the packages itself") was corrected before being treated as actually done — direct user restructuring (renaming `route-root.tsx`→`route.tsx`, `config/index.tsx`→`root.tsx`, splitting `documents.tsx`'s `Viewmodel` into its own `view-model.tsx`, scaffolding an empty `views/defineHandler.ts`) happened concurrently with the feedback and was kept as the new real file layout, not reverted.

- **Root cause of the "makes no sense" reaction**: the first pass did segment dispatch *inside* `RouteComponents`' render body via `useParams`+a hand-rolled switch (`get-route-data.ts`). Re-examining Payload's real, already-fetched source (`renderRoot`/`getRouteData`) more carefully: that's an **async function that resolves route data — and can call `notFound()`/`redirect()` — before any view component renders**, not logic living inside the rendered view itself. TanStack Start's `loader` (and `beforeLoad` for auth-gate decisions, `validateSearch` for typed query params) is the direct, idiomatic equivalent of that same shape within TanStack's framework — not a deviation from mimicking Payload, a *more correct* translation of it across frameworks (Next's RSC and TanStack Start's loader model solve the identical problem — resolve-before-render with redirect/notFound support — via different language mechanisms).
- **Corrected design, agreed, not yet implemented**: `defineHandler()` (in the now-real `views/defineHandler.ts`) returns the whole route config object `{ component: RouteComponents, loader }`, so `www/src/routes/(admin)/admin/$.tsx` becomes `createFileRoute('/(admin)/admin/$')(defineHandler())` — thinner than before, closer to Payload's real one-line `RootPage(...)` call. `loader` throws TanStack's real `notFound()` for an unmatched slug instead of a hand-rolled `'not-found'` view branch; `RouteComponents` reads the already-resolved `{ viewType, routeParams }` via `useLoaderData({ strict: false })` instead of re-deriving it itself.
- **URL scheme flattened, a deliberate divergence from Payload's own literal scheme**: `/admin/pages` not `/admin/collections/pages` — no `collections`/`globals` prefix at all, one flat slug namespace checked against both lists in the loader. Accepted tradeoff: a collection and a global can never share a slug.
- **`link.ts` deleted** — a one-symbol re-export file added no value; `Viewmodel` already imports `Link` directly from `@tanstack/react-router` (the user's own direct edit, kept). **Types move from `admin/types/index.ts` to `views/types.ts`** (flat file, not a folder) — an explicit correction to where admin-specific types should live, narrower than the general `types/index.ts`-folder convention in `CLAUDE.md`.
- Not yet implemented — `docs/PLAN.md`'s Sub-stage 1A was rewritten to describe this corrected design and reopened (unchecked), waiting for explicit go-ahead before any of it is actually built.

## 2026-09-05 (yet later) — 1A corrected a third time: a real nested TanStack router, not a shared loader

The loader-based design above was itself corrected again same day ("i want into feel like they are there own routes like an nested thing... putting eveyhing in one page will be very heavy in some use cases and too may requests for things you havent loade[d]") — the concern being that one shared `loader`+`component` behind the single file route means every view's code ships together regardless of which one is actually needed, and nothing about the structure reads as separate routes.

- **Verified before proposing anything**: TanStack Router's own code-splitting (`createLazyRoute`/`.lazy()`) only operates at the granularity of one file route — with deliberately only one file, that mechanism can't split further on its own. Checked whether a documented pattern exists for mounting a second, independent nested router under one file route; found nothing in TanStack's docs confirming or denying it directly.
- **User supplied the real, correct-looking `createRootRoute`/`createRoute({ getParentRoute })`/`.addChildren()`/`createRouter()` code-based-routing API directly** — confirmed for real (not taken on faith) by grepping the installed `@tanstack/react-router`/`router-core` packages: `basepath` is a genuine `createRouter` option (found in `router-core`'s real source), which is exactly what lets a second, code-based-built router mount under `/admin` while reading the same real browser URL as the outer app router — no custom history-sync code needed. `lazyRouteComponent`'s real signature was also confirmed directly from its `.d.ts` (`(importer, exportName?) => ...`).
- **One real structural conflict found while designing the tree, not glossed over**: collections and globals are siblings at the same URL depth (`/admin/:slug`), and two sibling routes can't both be a bare dynamic segment matching the same shape — so they share one `$slug` route whose `loader` disambiguates by checking both slug lists (same tradeoff as the earlier flattened-URL decision, now showing up as a routing-structure constraint rather than just a data-collision risk), with only the collection side getting a genuine nested `$id` child.
- **Full illustrative code sketch (`defineHandler.ts` building `adminRootRoute`→`dashboardRoute`/`slugRoute`→`idRoute`, `createRouter({ routeTree, basepath: '/admin' })`, mounted via a `<RouterProvider>`-rendering component) was written into `docs/PLAN.md`'s Sub-stage 1A** at the user's request ("lets show how in code it looks") — not yet applied to any real file, explicitly illustrative pending approval.
- **Flagged, not yet answered**: whether a nested `RouterProvider`'s matched loader actually executes during TanStack Start's SSR pass or only resolves client-side post-hydration — noted in the plan as something to verify empirically once actually built, not assumed either way.

## 2026-09-05 (final) — the nested-router design built for real, two real bugs found only by testing, one accepted gap

User confirmed via the two follow-up mid-turn messages ("yes both ssr and client" / "during ssr") that SSR-loader execution is a hard requirement, then asked directly to try the nested-router approach empirically rather than fall back to the safer design, once the earlier globals-prefix correction had resolved the sibling-route ambiguity. Built exactly to the corrected plan, then verified against the real dev server — and the first attempt genuinely did not work, exactly the risk that had been flagged going in:

- **First real bug, found by testing, not by reasoning about the API**: `use(loadPromise)` called directly inside `AdminRouterMount` with no ancestor `<Suspense>` boundary produced **completely empty output on every single route**, including the dashboard — no error, no fallback, nothing after `</header>` in the raw SSR HTML. `use()` needs a Suspense boundary *above* the component that calls it; a component can't wrap its own `use()` call in a `<Suspense>` and have that catch its own suspension. Fixed by splitting into an inner `AdminRouterInner` (calls `use()`) wrapped by the outer `AdminRouterMount` in `<Suspense fallback={null}>`.
- **Second real bug, also found only by testing**: after the Suspense fix, `/admin/pages`, `/admin/pages/abc123`, and `/admin/globals/headers` all rendered correctly — but `/admin` (the dashboard) alone still rendered empty. Root cause: the fallback path fed to `createMemoryHistory({ initialEntries: [...] })` on the server was `/admin` (no trailing slash); after `basepath: '/admin'` stripping, that leaves an empty string, which didn't match the dashboard route's `path: '/'`. Fixed by seeding `/admin/` (trailing slash) instead — confirmed via a direct before/after `curl` that this was exactly the cause, not a guess left unverified.
- **A stray "makes no sense" moment mid-debugging that was actually a stale dev server, not a new bug**: after a batch of file deletions (`route.tsx`, `types.ts`, `get-route-data.ts`), the dev server briefly threw `Failed to load url .../route.tsx` — a Vite module-graph reference to a file already removed from `views/index.ts`, just not yet invalidated. A full `bun run dev` restart (not a source fix) cleared it; confirmed via a fresh `tsc --noEmit` that the actual current source had no such reference.
- **Real evidence the original bundle-size goal was actually achieved**, not just structurally plausible: `tsdown`'s build log showed separate output chunks per lazy-loaded view (`documents-*.mjs`, `document-editor-*.mjs`, `global-editor-*.mjs`, `collection-list-*.mjs`, plus a tiny standalone `data-*.mjs` for the `collections`/`globals` arrays extracted specifically so `route-tree.ts`'s loaders don't have to eagerly pull in the `Documents` component's own code just to validate a slug) — this is the concrete mechanism that answers "too many requests for things you haven't loaded" from the start of this whole sub-stage.
- **One gap accepted, not silently dropped**: the nested router's `notFound()` throw renders correctly (TanStack's own default `<p>Not Found</p>`, since no custom `notFoundComponent` was configured) but the *outer* HTTP response for a bad slug still comes back 200, not 404 — the nested router's not-found state has no path back to the real outer file-route's response status. Recorded as a known, accepted limitation in `docs/PLAN.md`, not fixed, since it's lower-stakes for an authenticated admin panel than it would be for public content.
- Sub-stage 1A is now checked off in `docs/PLAN.md`, verified via `curl` against real SSR output across all five test URLs, not assumed from a passing typecheck alone.

## 2026-09-05 (last) — Sub-stage 1B built; a real-time naming/consolidation pass settled the final file shape

1B (the real collection list) was built, then went through a rapid back-and-forth of direct naming/structure corrections while the dev server stayed live — each one verified via `tsc --noEmit` + `curl` immediately after, not batched:

- **"i truely hate this kind of export, export { defineHandler }"** — corrected the `const x = ...; export { x }` split pattern (present in `defineHandler.ts`, `route-tree.ts`, `documents/data.ts`) to plain inline `export const`/`export function`. `CLAUDE.md`'s exports rule refined to make explicit this was never about single-declaration files.
- **"use the views/pages for the pages respectively... i have created placeholders"** — the user directly scaffolded empty `views/pages/{collections.tsx, collections.uuid.tsx, globals.tsx}` stub files as the exact target shape; matched their content into those locations (moving off the `collection-list/`, `document-editor/`, `global-editor/` folders built moments earlier).
- **"all pages NameComponent() get export function GlobalsComponent() {}"** — every page component renamed to `{PageName}Component`.
- **"collections can be foldered as collections/index.tsx, uid.tsx"** — `pages/collections.tsx`/`collections.uuid.tsx` restructured into `pages/collections/{index.tsx, uid.tsx}`.
- **"this becomes CollectionsUuidComponent = UUIDComponent or uuidComponent"** — settled on `UUIDComponent` (must be PascalCase to work as JSX; `uuidComponent` would be parsed as a DOM tag name, not a component).
- **"why does collections have `{ slug }: CollectionsProps` and not `Route.useParams()`"** — real inconsistency caught: `CollectionsComponent` was the only page component receiving its data as a prop from a parent instead of reading it internally like `GlobalsComponent`/`UUIDComponent` do. Fixed to read via `useLoaderData({ strict: false })` itself; the parent (`CollectionView`, at the time) simplified to stop passing anything down.
- **Real SSR bug, found only by testing**: after that, `/admin/pages` alone started throwing `Cannot read properties of null (reading 'useContext')` during SSR (falling back to client-only rendering) — root cause was a hand-rolled `React.lazy()` + manual `<Suspense>` (in the since-merged `collection-view.tsx`), the one page-loading path not using TanStack's own `lazyRouteComponent()`. Fixed by switching to `lazyRouteComponent()` like the other three page components, which has correct SSR/Suspense wiring built in.
- **"but we lost our article top level container flex flex-col w-full md:max-w-4xl"** — the shared container wrapper (previously in the now-deleted single-page `route.tsx` dispatcher) had no home in the new nested-router world. Fixed at the right place: `adminRootRoute`'s own `component` in `route-tree.tsx`, wrapping `<Outlet/>` once for every page uniformly, rather than duplicating it per-page.
- **"packages/baseconfig/.../admin-router-mount.tsx should be directly inside defineHandler... we are now reducing the files... so we dont have lots of files"** — `AdminRouterMount`/`AdminRouterInner` merged directly into `defineHandler.tsx` (renamed `.ts`→`.tsx` since it now holds JSX), `admin-router-mount.tsx` deleted. **General preference, worth remembering**: fewer, consolidated files over one-thing-per-file splitting for tightly-coupled pieces that are never used apart — don't default to maximal fragmentation.
- **"whats this for packages/baseconfig/src/admin/views/collection-view"** then **"then that goes inside pages its a page"** — `CollectionView`'s dispatch logic (list vs. nested `$id` `<Outlet/>`) merged directly into `pages/collections/index.tsx`'s `CollectionsComponent` — one file doing both jobs, `collection-view/` folder deleted, `route-tree.tsx`'s `collectionRoute` now points `lazyRouteComponent(() => import('./pages/collections'), 'CollectionsComponent')` directly.
- **`AdminRouterInner`→`InnerRouter`, `AdminRouterMount`→`RouterMount`** — final naming polish inside `defineHandler.tsx`.
- Final settled file tree and every fix verified is recorded directly in `docs/PLAN.md`'s Sub-stage 1A+1B entry (merged into one, since so much of 1A's original file layout was superseded during 1B) rather than duplicated here.
- **One more real gap caught only by re-running `www`'s own typecheck at the very end** (not `packages/baseconfig`'s — that one stayed clean throughout and never would have caught this): every `Link` pointing at the nested router's own paths (`/$slug`, `/globals/$slug`, `/$slug/$id`, in `components/view-model.tsx` and `pages/collections/index.tsx`) fails `www`'s stricter `tsc` — `www`'s own `Register` (declared in `www/src/router.tsx`) only knows the *outer* app's three real routes (`/`, `/admin`, `/admin/$`), and since `www/tsconfig.json`'s path mapping pulls `packages/baseconfig`'s raw source into `www`'s own compile, `Link`'s strict `to` typing gets checked against that outer tree even for `Link`s that only ever render inside the separate nested router. The paths are correct at runtime (confirmed via `curl` throughout this whole sub-stage) — this is a real, permanent mismatch between the two routers' type registrations, not a bug to "fix" by changing the path. Resolved with `as any` casts on `to`/`params` at each such call site (this codebase's `biome.json` already disables `noExplicitAny`). **This will recur in Sub-stage 1C** — any new `Link` rendered inside the nested router pointing at a nested-router-only path needs the same cast. Lesson for next time: re-run `www`'s own `tsc --noEmit` after *any* change to a `Link`'s `to`/`params`, not just `packages/baseconfig`'s.

## 2026-09-05 (still later) — globals keep their `/globals/` prefix; this also resolves the sibling-route ambiguity as a side effect; SSR-loader execution confirmed required, not just checked

Two small, high-value corrections to the tree above:

- **"globals remains globals/${slug} its only collections that different"** — globals keep the literal `globals/` URL prefix; only collections dropped theirs. This wasn't purely a URL-taste call: it also cleanly resolves the sibling-dynamic-route ambiguity flagged in the previous entry, since `'globals/$slug'` (static-then-dynamic) and `'$slug'` (bare dynamic) are structurally distinct paths at the same tree depth — no more shared `$slug` route disambiguating collection-vs-global via a shared loader. `docs/PLAN.md`'s tree and code sketch updated: `globalRoute` (`path: 'globals/$slug'`) is now a clean sibling of `collectionRoute` (`path: '$slug'`, still the only one with a nested `$id` child), each with its own single-purpose `loader`.
- **"yes both ssr and client" / "during ssr"** — confirmed as an explicit requirement, not merely something to check out of curiosity: the nested router's matched `loader` must actually execute during TanStack Start's SSR pass, producing correct initial HTML, not just resolve client-side after hydration. `docs/PLAN.md` now states this as a requirement to verify true (and fix if false) once built, rather than an open question either way is acceptable.
- Still not implemented — plan only, per "first plan and stage and wait my approval."
